# Set default behavior to automatically normalize line endings to LF
* text=auto eol=lf

# Explicitly ensure shell scripts always stay LF (Crucial for Azure Cloud Shell!)
*.sh text eol=lf

# Explicitly handle Windows specific scripts if they ever exist
*.bat text eol=crlf
*.ps1 text eol=crlf
